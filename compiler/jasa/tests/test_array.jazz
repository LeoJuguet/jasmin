

fn test_array(reg u64 entry1 entry2) -> reg ptr u64[2]
{
    reg u64 a;
    reg u32 c;
    reg u64[2] arr;

    arr[0] = entry1;
    arr[1] = entry2;


    c = arr[0];
    arr[u32 0] = arr[u32 1];
    arr[u32 1] = c;

    a = arr[1];
    arr[0] = arr[1];
    arr[1] = a;

    c = arr[0];
    arr[u32 0] = arr[u32 1];
    arr[u32 1] = c;


    return arr;
}
